{
  "name": "to-utf",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test --test-concurrency=1"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
